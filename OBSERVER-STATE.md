# Fade-App Observer State
Last updated: 2026-04-26  ·  Run 16

---

## Active development areas

Six commits landed after Run 15, completing the sprint (threads 1–3) that was in progress at the prior observation. The commits closed two documentation gaps flagged in Runs 14–15 (analysis-result race entry removed at `71f5d93`; Codex artifacts committed at `e88da83`), swept additional encoder-constraint codec paths into `full_sweep.rs` (`1dbf064`), migrated `video.rs` and `audio.rs` run() wrappers to the `window_progress_emitter_batched` helper (`af0c7b6`, `263d7d2`), and updated SESSION-STATUS to reflect closure (`01940d9`).

No arc is in flight. SESSION-STATUS confirms clean post-sprint state with no open tasks.

## Fragile / high-risk areas

The **Blender backend path resolution** residual fragility is unchanged. `fc1d9d3` covered macOS bundle and Linux FHS paths; the general "not hardened for all deployment contexts" caveat in SESSION-STATUS persists with no new incidents.

**`archive.rs` run() wrapper is intentionally inline** — the `window_progress_emitter_batched` helper assumes a 0–100 progress scale; archive emits a different scale. A TODO was left in the code. This is a named, acknowledged deferral, not a silent gap, but it breaks the uniformity of the emitter migration and may create maintenance confusion later.

**`src/lib/types/generated/ConvertOptions.ts` has an uncommitted hand-edit.** The file is generated by ts-rs and carries a "Do not edit this file manually" header. The working-tree diff inserts a JSDoc comment (`/** Requires output resolution ≥ 1280×720… */`) mid-struct, inline with the `dnxhr_profile` field. This comment will be clobbered silently on the next `cargo build` with ts-rs codegen enabled. The documentation intent is correct — the constraint is real — but the location is wrong.

## Deferred work accumulation

**HAP sub-format compatibility sweep** is still unswept. DNxHD bitrate and CineForm quality were added to `full_sweep.rs` at `1dbf064`, but HAP (hap, hap_alpha, hap_q sub-formats) have not been swept for encoder constraints.

**Three off-ledger items** persist across multiple runs with no movement: STEP/IGES format support, `librewin_common` superset-vs-authoritative strategy, and the `createLimiter` formal slot-leak watchdog.

The ts-rs codegen `ConvertOptions.ts` hand-edit (described above) represents a documentation approach that needs to be redirected — either to the Rust struct's `#[ts(doc)]` attribute or to a separate domain doc — before the next codegen run overwrites it.

## Pattern watch

All five KNOWN-BUG-CLASSES (BC-001 through BC-005) are documented and resolved. No new pattern class is emerging from recent commit activity. The `$bindable()` cascade pattern in `CLAUDE.md` has not recurred. The DNxHR minimum-resolution guard (BC-005 encoder-constraint class) is now covered in `full_sweep.rs` alongside DNxHD and CineForm.

## CI health

No GitHub remote is configured for this repo; CI status cannot be verified via `gh`. The most recent SESSION-STATUS and commit history consistently report green CI across all arcs. The CI streak has persisted through the full sprint closure. No flaky steps or recurring failures are noted in any recent artifact.

## Observer notes

**Two persistent documentation gaps are now closed.** The analysis-result race entry in Known Risks (stale since `ce5b37e` in Run 12) was removed at `71f5d93`. The untracked Codex workspace (flagged in Runs 10–15) was committed at `e88da83`. Both were logged across multiple runs; both are now resolved.

**Hand-edit in a generated file is the most actionable finding this run.** `ConvertOptions.ts` has a JSDoc comment that will be silently clobbered on next `cargo build`. If this constraint annotation is worth preserving — and it is — it belongs in the Rust source (`#[doc = "..."]` on the struct field, which ts-rs can propagate) or in a domain doc, not in the generated output.

**Git note protocol remains absent across the entire commit history.** No structured `git notes` on any commit in the 40-commit window. This gap has persisted across at least 9 consecutive Observer runs.

**macOS 26 beta (Darwin 25.4.0)** — local CT execution remains unreliable; CI is the only truth for CT results. Carries forward from prior runs.
