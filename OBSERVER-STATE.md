# Fade-App Observer State
Last updated: 2026-04-28  ·  Run 17

---

## Active development areas
Twenty-one commits since Run 16 closing three back-to-back sprints (TASK-1 through TASK-21). The arc concentrated on `src-tauri/src/args/video.rs`, which absorbed four substantive encoder-correctness fixes: a split h264|h265 profile branch with a new `h265_effective_profile()`, a yuv444p/high444 auto-promotion when h264 crf=0, a DNxHD resolution guard with DNxHR contract annotation, and an AV1 default-encoder switch from libaom-av1 to libsvtav1. Parallel security work hardened temp-file handling — per-job mkdtemp 0700 sandboxes landed in renderer-facing preview paths and four remaining medium-priority sites. Earlier in the window, archive, fs_commands, IPC entry, subtitle filter, merge, and DuckDB paths each took targeted hardening commits. Sweep coverage grew alongside the fixes (DNxHD bitrate, CineForm quality, HAP sub-formats added to `full_sweep.rs`). No arc currently in flight.

## Fragile / high-risk areas
`args/video.rs` is the bug-fix concentration point — five commits in this window touched it, all closing distinct BC-005 instances. The module is now the most-churned file across the sprint and warrants pattern attention even though every fix landed CI-green. The 64×64 sweep fixture continues to bypass DNxHD/DNxHR resolution guards because the fixture sets no explicit resolution; the guard itself is correct, the test surface is the gap. Three encoder-availability OPEN entries (HAP, libtheora, libaom-av1) reflect Homebrew FFmpeg 8.1 build flags rather than code defects, but they remain unresolvable on standard macOS dev installs. Blender backend path resolution fragility persists unchanged from Run 16. The uncommitted hand-edit in `src/lib/types/generated/ConvertOptions.ts` flagged in Run 16 still shows as modified in git status — it survived another sprint without being redirected to source.

## Deferred work accumulation
Explicit deferral noted in SESSION-STATUS: av1_speed/-cpu-used → -preset remapping for libsvtav1, post-encoder-switch follow-up. The h265-lossless case is explicitly deferred. The 64×64 DNxHD/DNxHR sweep failures are accepted as fixture-shape rather than code-shape issues. The ConvertOptions.ts hand-edit deferral is now two runs old. STEP/IGES format support, `librewin_common` superset-vs-authoritative strategy, and `createLimiter` slot-leak watchdog remain off-ledger across multiple runs. Sweep tests carrying `#[ignore]` are manual-only by design, not deferred work, but their coverage value depends on someone running them — TASK-15 and TASK-16 demonstrated the pattern works when exercised.

## Pattern watch
BC-005 (encoder-constraint) drove the bulk of this sprint with four distinct instances closed (h264 lossless, h265 profile mismatch, DNxHD resolution coupling, AV1 encoder unavailability). The class is now well-documented and the fix pattern is consistent: codec-aware arg builders with capability checks before emission. No new bug class emerged. The `$bindable()` cascade pattern has not recurred since the TASK-11 audit. The validate-before-interpolate pattern was applied again in the DuckDB SQL path (`4f4ed5c`). Temp-file sandboxing via per-job mkdtemp 0700 is now the established pattern across both renderer-facing and medium-priority sites.

## CI health
All five most recent runs completed successfully. Green across the entire 21-commit sprint window. SESSION-STATUS and commit messages consistently report CI-green at sprint close. No flaky steps observed. The Run 16 note that `gh` could not verify CI status appears outdated — five recent runs report completed/success, indicating the remote and CI are now reachable from the observer perspective.

## Observer notes
Three sprints closing 21 tasks in roughly three days represents unusually high throughput for this repo; momentum is strong and direction is clearly defensive (security, correctness, sweep coverage) rather than feature-led, consistent with the 0.6→0.8 testing-phase cadence. Continuity check: Run 16 flagged the ConvertOptions.ts hand-edit as the most actionable finding — it remains uncommitted and unresolved one run later, still visible in `git status`. Run 16 also flagged HAP sweep coverage as missing; that gap closed via `4a8049e` but surfaced an environment limitation rather than a code path. INVESTIGATION-LOG hygiene drift: two OPEN entries (h264 lossless and h265 profile) describe conditions that were closed by `ea93db0` and `fa74e91` but were not updated to CONFIRMED. The log is now mixing resolved and unresolved states, which weakens its signal. Git notes remain absent across the entire commit window — protocol gap persists across ten consecutive runs. No artifact files missing.
